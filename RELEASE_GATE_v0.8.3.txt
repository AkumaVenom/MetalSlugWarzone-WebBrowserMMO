Metal Slug Warzone v0.8.3 — Combat Facing Consistency
XAMPP Test Candidate — Static Release Gate

STATUS
- Static/code validation: PASS
- Runtime XAMPP/browser facing acceptance: REQUIRED before baseline promotion
- Schema revision: 8 (unchanged; no migration)

CORRECTIVE SCOPE
- Normal PvE Security Backup Squad members on the left are mirrored at render time so they face right toward the enemy.
- The normal PvE Commander explicitly uses the existing right-facing character sprite.
- PvP local Commander presentation explicitly uses the character's right-facing sprite.
- PvP opposing Commander presentation explicitly uses the character's left-facing sprite.
- The same PvP renderer covers live human-vs-human, Live AI and Quick AI Duel / snapshot modes.
- Trevor's existing mirror_left catalog fallback is honored because no separate Trevor left-facing sprite file exists.
- Application version is 0.8.3 so versioned CSS/JS URLs refresh automatically.
- No sprite or other runtime image asset is generated, duplicated, flipped on disk, or modified.

STATIC VALIDATION
- 60/60 PHP files pass `php -l`.
- `public_html/assets/js/msw.js` passes `node --check`.
- `public_html/assets/css/msw.css` brace structure is balanced.
- Security backup markup emits right-facing + render-time mirror classes.
- PvP local Commander resolves `sprite_r`; PvP opponent resolves `sprite_l` with the existing `mirror_left` fallback.
- All six character catalog entries resolve valid right/left battle assets; Trevor is the only declared mirror-left fallback.
- `battle-sprite-mirror-x` uses a protected horizontal `scaleX(-1)` transform.
- 67/67 runtime PNG/JPG/JPEG/WebP/GIF asset hashes are byte-identical to v0.8.2.
- `battle_engine.php`, `pvp_engine.php`, schema runtime, install schema and `msw.js` are byte-identical to v0.8.2.

RELEASE-BLOCKING BROWSER CHECKS
1. Open a normal field/mission battle with one or two Security Backup Squad members assigned.
2. Confirm the Commander and both Security escorts on the left face right, toward the enemy.
3. Trigger covering fire/interception and confirm the existing hit/assist animations remain intact.
4. Start a human-vs-human PvP battle. From each player's own browser, confirm the local Commander is left/facing right and the opponent is right/facing left.
5. Repeat against an AI commander in Live AI mode.
6. Repeat with Quick AI Duel / snapshot mode.
7. Test Trevor once as the opponent and confirm the mirror fallback makes him face left without altering the source sprite file.
8. Confirm HP, attacks, turns, AI actions, victory, XP and persistence are unchanged.
9. Recheck one automatic FOB/Dispatch result battle and confirm the accepted v0.8.2 result-facing behavior remains unchanged.
