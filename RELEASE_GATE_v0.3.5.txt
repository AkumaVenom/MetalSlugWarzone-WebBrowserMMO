Metal Slug Warzone v0.3.5 — Local WorldServer Console
======================================================
Parent: v0.3.4 — Level-1 Fulton Manufacturing (runtime-confirmed)
Schema: 4 (unchanged)
Status: PASS — STATIC / ISOLATED CONSOLE SUBSYSTEM XAMPP TEST-CANDIDATE GATE

Verified pre-package checks
---------------------------
PASS — 47/47 PHP files: php -l
PASS — 1/1 JavaScript files: node --check
PASS — CSS structure: 401 opening / 401 closing braces
PASS — local feed resides outside public_html
PASS — deny-all _server_console/.htaccess defense in depth
PASS — human identity event accepted / bot identity rejected
PASS — map movement route event rejected
PASS — successful authenticated WEB traffic accepted
PASS — movement traffic and HTTP 404 traffic suppressed
PASS — 60/60 concurrent NDJSON writes preserved
PASS — 8 MiB feed rotation verified
PASS — no raw direct-message body reaches console hook
PASS — no PHP/Apache/MySQL error-log ingestion code
PASS — five movement/presence/state-poll endpoints hard-suppressed
PASS — combat console emission deferred until DB commit
PASS — schema revision 4 unchanged / no SQL migration
PASS — v0.3.4 Fulton thresholds 1 / 4 / 8 / 15 preserved
PASS — 31/31 runtime images byte-identical to v0.3.4
PASS — 54 literal PHP include/require references resolve
PASS — 28 literal msw_url() references resolve
PASS — 0 source_assets directories / 0 nested ZIP archives

Release delta contract
----------------------
- Adds serverconsole.bat + serverconsole.ps1 as a local server-PC WorldServer-style console.
- Adds private NDJSON human activity feed outside public_html; no remote admin endpoint exists.
- Shows successful authenticated human site traffic with username/user ID/IP/method/route/latency.
- Adds color-coded action channels for AUTH, COMBAT, RECOVERY, MISSION, R&D, BASE,
  DISPATCH, STRATEGIC, FOB, PVP, SOCIAL and PROFILE activity.
- Excludes bot activity from the human operator feed.
- Excludes all warzone/Mother Base movement and presence polling, plus PvP state polling.
- Does not ingest technical error/exception logs and does not expose sensitive request payloads.
- Rotates the local feed at 8 MiB and retains three archived generations.
- Preserves v0.3.4 Level-1 Fulton manufacturing and all prior gameplay/persistence contracts.

Runtime acceptance boundary
---------------------------
Native Windows cmd/PowerShell and the user's live XAMPP/MariaDB/browser environment are not
available in the build container. Run docs/XAMPP_TEST_PLAN.md and confirm real console colors,
local live-follow behavior, human/IP attribution, action coverage, and complete movement silence
before promoting v0.3.5 to stable.
